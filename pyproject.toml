[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "urban-accessibility-thesis"
version = "0.1.0"
description = "Placeholder scaffold for an urban accessibility thesis pipeline"
readme = "README.md"
requires-python = ">=3.11"
