# Archived Scripts

These scripts are preserved for provenance and old experiments, but they are not
part of the final thesis workflow.

```text
analyze_shap_cluster_equity.py   post-hoc ACS/equity exploration
build_model_comparison_report.py older compact comparison report builder
build_reduced_feature_set.py     earlier reduced-feature selection utility
build_shap_report.py             older SHAP report/figure builder
explain_multicity_gnn.py         custom GNNExplainer-style masks before PyG
plot_reduced_xgboost_pdp.py      older PDP plotting workflow
tune_reduced_xgboost.py          exploratory XGBoost hyperparameter search
```

The active workflow is documented in `../README.md`.
